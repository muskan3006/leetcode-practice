package twoPointer

object LongestPalindromicSubstring extends App {

  /**
   * Given a string s, return the longest palindromic substring in s.
   *
   *
   *
   * Example 1:
   *
   * Input: s = "babad"
   * Output: "bab"
   * Explanation: "aba" is also a valid answer.
   * Example 2:
   *
   * Input: s = "cbbd"
   * Output: "bb"
   *
   *
   * Constraints:
   *
   * 1 <= s.length <= 1000
   * s consist of only digits and English letters.
   */

//  def longestPalindrome(s: String): String = {
//    val size = s.length
//
//    def isPalindrome(start: Int, end: Int): Boolean = {
//      if (start >= end) true else if (s(start) == s(end)) isPalindrome(start + 1, end - 1) else false
//    }
//
//    def helper(centre: Int, start: Int, end: Int, length: Int, string: String): String = {
//
//    }
//  }
}
